# Base de données SQLite (fichier local, aucune installation requise)
DATABASE_URL="file:./dev.db"

# Secret NextAuth — générez avec : openssl rand -base64 32
NEXTAUTH_SECRET="votre-secret-ici"
NEXTAUTH_URL="http://localhost:3000"
